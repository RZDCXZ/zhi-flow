# 助手 Message 生成独立于 SSE 订阅

助手 Message 生成 module 拥有从创建到终态的完整生命周期，持久化状态是终态的唯一事实来源，并由数据库保证同一 Conversation 同一时刻最多一个正在生成的 Assistant Message。SSE 只订阅生成事件，连接断开不会被视为用户取消；这一取舍可能让生成在用户断线后继续消耗资源，但能避免传输状态覆盖领域状态，并支持跨实例取消、终态竞争和遗留恢复。
